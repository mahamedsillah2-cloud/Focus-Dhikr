<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      PRIVACY: android.permission.INTERNET is deliberately NOT declared.
      Without it the process cannot open a socket, so no analytics, no crash
      reporting, no ads and no trackers are technically possible. Verify with:
          aapt dump permissions app-debug.apk
      Do not add it. See docs/PRIVACIDAD.md.
    -->

    <!-- Read per-app foreground time. appop permission: granted by the user in
         Settings > Special app access > Usage access. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Draw the pause screen over other apps. Also the officially documented
         exemption that lets us start an Activity from the background on API 29+. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Keep the usage accounting loop alive. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Restart accounting after reboot / app update. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Let the user pick ANY installed app, not a hardcoded list. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Re-evaluate schedule windows (e.g. 22:00-08:00) at their exact edges. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Offered as a prompt, never required. Improves reliability under Doze. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Fallback discovery when QUERY_ALL_PACKAGES is unavailable. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".FocusDhikrApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusDhikr">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.FocusDhikr">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The progressive pause screen. excludeFromRecents + noHistory so it never
          lingers in the recents stack; taskAffinity="" keeps it out of the main task
          so dismissing it does not drop the user into the app's normal UI.
        -->
        <activity
            android:name=".ui.gate.GateActivity"
            android:exported="false"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:noHistory="false"
            android:showOnLockScreen="false"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.FocusDhikr.Gate" />

        <service
            android:name=".service.UsageTrackingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuously accounts the user's own app foreground time against
                               self-imposed daily limits and shows the reflection screen at the
                               moment a limited app is opened. All processing is on-device." />
        </service>

        <!--
          Instant interception. Without this the app falls back to ~1s polling.
          On Android 13+ this is a restricted setting for sideloaded APKs: see
          docs/INSTALACION.md.
        -->
        <service
            android:name=".service.AppBlockAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.ScheduleAlarmReceiver"
            android:exported="false" />

        <!--
          OPTIONAL and disabled by default. While active, Android refuses to
          uninstall the app until the user deactivates it in
          Settings > Security > Device admin apps. Pure friction, fully reversible.
        -->
        <receiver
            android:name=".service.FocusDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
